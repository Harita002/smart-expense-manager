spring.application.name=Expense-Trackers

spring.datasource.url=jdbc:mysql://localhost:3306/expense_db
spring.datasource.username=root
spring.datasource.password=@har1568
# springboot uses this credentials to create a datasource bean
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
# This controls how Hibernate handles database tables.
# Update schema automatically
spring.jpa.show-sql=true
# prints sql queries in console
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

server.port=8080
# This sets application port.